@page
@model Shopify.Presentation.RazorPages.Pages.Shared.AccessDeniedModel
@{
    Layout = "";
}



<!doctype html>
<html lang="fa" dir="rtl">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width,initial-scale=1" />
    <title>دسترسی غیرمجاز — Access Denied</title>
    <!-- Vazirmatn Font -->
    <link href="https://fonts.googleapis.com/css2?family=Vazirmatn:wght@300;400;600;700&display=swap" rel="stylesheet">
    <!-- Tailwind CDN -->
    <script src="https://cdn.tailwindcss.com"></script>
    <script>
        tailwind.config = {
          theme: {
            extend: {
              fontFamily: { sans: ['Vazirmatn', 'ui-sans-serif', 'system-ui'] },
              colors: {
                tealDark: '#0f1724',
                glass: 'rgba(255,255,255,0.06)'
              }
            }
          }
        }
    </script>
    <style>
        body {
            background: radial-gradient(1000px 400px at 10% 10%, rgba(99,102,241,0.08), transparent), linear-gradient(120deg,#071025 0%, #102a33 50%, #12343f 100%);
        }

        .glass {
            background: linear-gradient(180deg, rgba(255,255,255,0.04), rgba(255,255,255,0.02));
            backdrop-filter: blur(8px) saturate(120%);
            border: 1px solid rgba(255,255,255,0.06);
        }

        keyframes floaty {
            0% {
                transform: translateY(0)
            }

            50% {
                transform: translateY(-10px)
            }

            100% {
                transform: translateY(0)
            }
        }
    </style>
</head>
<body class="min-h-screen flex items-center justify-center p-6 text-gray-100">
    <div class="w-full max-w-4xl glass rounded-2xl shadow-2xl overflow-hidden grid grid-cols-1 md:grid-cols-2">

        <!-- Illustration / Left -->
        <div class="flex items-center justify-center p-10 bg-gradient-to-br from-transparent to-black/10">
            <div class="text-center">
                <div class="mx-auto mb-6 w-36 h-36 rounded-full flex items-center justify-center bg-white/6 border border-white/8 shadow-lg animate-[floaty_6s_ease-in-out_infinite]">
                    <!-- Lock SVG -->
                    <svg xmlns="http://www.w3.org/2000/svg" class="w-20 h-20 text-white" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.2">
                        <rect x="3" y="11" width="18" height="11" rx="2" ry="2"></rect>
                        <path d="M7 11V7a5 5 0 0 1 10 0v4"></path>
                        <circle cx="12" cy="16" r="1.4" fill="currentColor"></circle>
                    </svg>
                </div>

                <h1 class="text-2xl md:text-3xl font-bold mb-2">دسترسی غیرمجاز</h1>
                <p class="text-sm md:text-base text-gray-300 max-w-xs mx-auto">شما اجازهٔ مشاهدهٔ این صفحه را ندارید. برای دسترسی باید وارد حساب کاربری شوید یا از سطح دسترسی مناسب استفاده کنید.</p>
            </div>
        </div>

        <!-- Content / Right -->
        <div class="p-8 md:p-10 flex flex-col justify-between">
            <div>
                <h2 class="text-lg font-semibold mb-3">چه اتفاقی افتاد؟</h2>
                <ul class="space-y-2 text-gray-200">
                    <li class="flex items-start gap-3"><span class="inline-block mt-1 w-3 h-3 rounded-full bg-red-400/90"></span>احتمالاً وارد سیستم نشده‌اید.</li>
                    <li class="flex items-start gap-3"><span class="inline-block mt-1 w-3 h-3 rounded-full bg-amber-400/90"></span>سطح دسترسی شما برای این بخش کافی نیست.</li>
                    <li class="flex items-start gap-3"><span class="inline-block mt-1 w-3 h-3 rounded-full bg-sky-400/90"></span>لینک منقضی شده یا مسیر مستقیم غیرمجاز است.</li>
                </ul>

                <div class="mt-6 flex flex-wrap gap-3">
                    <a href="#" onclick="history.back(); return false;" class="inline-flex items-center gap-2 px-4 py-2 rounded-full border border-white/10 text-sm font-medium hover:bg-white/5 transition">بازگشت</a>
                    <a href="/login" class="inline-flex items-center gap-2 px-4 py-2 rounded-full bg-gradient-to-r from-indigo-500 to-sky-500 text-sm font-semibold shadow-md">ورود به حساب</a>
                    <a href="/" class="inline-flex items-center gap-2 px-4 py-2 rounded-full bg-white/6 border border-white/6 text-sm">صفحهٔ اصلی</a>
                </div>
            </div>

            <div class="mt-8 flex items-center justify-between text-sm text-gray-300">
                <div>در صورت اشتباه بودن، با پشتیبانی تماس بگیرید: <a href="mailto:admin@example.com" class="underline">admin@example.com</a></div>
                <div class="text-gray-200 font-semibold">کد: <span class="text-indigo-300">403</span></div>
            </div>
        </div>

    </div>

    <!-- small script for RTL support if needed -->
    <script>
        document.documentElement.dir = 'rtl';
    </script>
</body>
</html>