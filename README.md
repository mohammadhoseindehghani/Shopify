## 🛒 Shopify Clone - C# E-commerce Platform

> این پروژه یک پلتفرم کامل فروشگاه اینترنتی است که بر اساس **معماری پیازی (Onion Architecture)** و با استفاده از **C#/.NET** توسعه داده شده است. هدف اصلی ارائه یک تجربه‌ی خرید ساده، سریع، و مطمئن با تمرکز بر مدیریت موجودی حساب کاربری به جای درگاه پرداخت مستقیم است.

---

### 🌟 ویژگی‌های کلیدی

* **معماری Onion (پیازی):** ساختاردهی اصولی پروژه با تفکیک واضح مسئولیت‌ها و وابستگی به سمت داخل.
* **Aggregates سازماندهی‌شده:** تفکیک Entityها، DTOها، Enumها و Contractsها در پوشه‌های مجزا (Aggregates) برای خوانایی بهتر در لایه Core.
* **رابط کاربری Razor Pages:** استفاده از **ASP.NET Core Razor Pages** برای توسعه‌ی لایه‌ی Endpoint و فرانت‌اند، جهت ارائه‌ی سریع و ساده.
* **احراز هویت کوکی-بیس (Cookie-Based Authentication):** سیستم امن ورود و ثبت‌نام کاربران با مدیریت سشن‌ها.
* **خرید مبتنی بر موجودی حساب:** قابلیت نهایی‌سازی سفارش با کسر از موجودی داخلی کاربر (بدون نیاز به درگاه پرداخت).
* **رابط کاربری زیبا و ریسپانسیو:** توسعه داده شده با فریم‌ورک **Bootstrap** برای سازگاری کامل با تمامی دستگاه‌ها.

---

### 💻 فناوری‌های استفاده شده

این پروژه با تکیه بر اکوسیستم **.NET** و ابزارهای مرتبط توسعه یافته است:

#### 🛠️ بک‌اند (Backend & Core)

| فناوری | نقش |
| :--- | :--- |
| **C# / .NET** | زبان و فریم‌ورک اصلی سمت سرور. |
| **Onion Architecture** | الگوی معماری اصلی پروژه. |
| **ASP.NET Core** | برای پیاده‌سازی سرویس‌ها و لایه‌ی Endpoint. |
| **(Database)** | **SQL Server** |

#### 🎨 فرانت‌اند (Frontend & Presentation)

| فناوری | نقش |
| :--- | :--- |
| **Razor Pages** | لایه‌ی Presentation/Endpoint برای ایجاد صفحات وب تعاملی. |
| **Bootstrap** | فریم‌ورک CSS برای طراحی واکنش‌گرا و اجزای رابط کاربری. |

---

### 🏛️ ساختار معماری (Onion Architecture)

پروژه به لایه‌های مجزا تقسیم شده است تا اصول معماری پیازی رعایت شود:

#### ۱. لایه‌ی Domain (هسته)
این لایه شامل منطق اصلی و قوانین کسب و کار است و به لایه‌های بیرونی وابسته نیست.

* `Domain.Core`: شامل Entityها (مثل `CartAgg`, `OrderAgg`, `ProductAgg` و `UserAgg` در پوشه‌های مجزا)، DTOها، و Contractهای اصلی.
* `Domain.Service`: شامل منطق‌های پیچیده‌ی کسب و کار که چندین Entity را درگیر می‌کند.
* `Domain.AppService`: شامل Use Caseها و تعاملات لازم برای اجرای درخواست‌های کاربر.

#### ۲. لایه‌ی Infrastructure
مسئول مدیریت ذخیره‌سازی داده‌ها (مانند Entity Framework Core) و پیاده‌سازی سرویس‌های خارجی.

#### ۳. لایه‌ی Endpoint (Presentation)
شامل رابط کاربری و نقطه‌ی ورود درخواست‌ها.

* از **Razor Pages** برای نمایش صفحات، مدیریت ورودی‌ها، و فراخوانی `Domain.AppService` استفاده شده است.

---

### 🛡️ پنل مدیریت (Admin Panel)

پنل ادمین که در لایه‌ی Endpoint پیاده‌سازی شده، دسترسی کامل به مدیریت منابع اصلی سیستم را فراهم می‌کند:

* **مدیریت محصولات:** ایجاد، ویرایش و حذف محصولات.
* **مدیریت دسته‌بندی‌ها:** سازماندهی و تعریف دسته‌بندی‌های جدید.
* **مدیریت کاربران:** مشاهده و مدیریت حساب‌های کاربری موجود.

---

### 📸 نمایه‌هایی از برنامه

در این بخش می‌توانید چند اسکرین‌شات از رابط کاربری برنامه (فرانت‌اند و پنل ادمین) قرار دهید.

| صفحه‌ی اصلی | سبد خرید | پنل ادمین (مدیریت محصولات) |
| :---: | :---: | :---: |
| `![Homepage Screenshot](assets/images/homepage.png)` | `![Cart Screenshot](assets/images/cart.png)` | `![Admin Panel Screenshot](assets/images/admin-products.png)` |
**

---

### 🚀 راهنمای نصب و راه‌اندازی

برای اجرای این پروژه در محیط توسعه‌ی محلی، مراحل زیر را دنبال کنید:

#### ۱. پیش‌نیازها

* **NET SDK (.NET 10)** 
* **SQL Server** 


#### ۲. شبیه‌سازی (Clone) ریپازیتوری

```bash
git clone [https://github.com/mohammadhoseindehghani/Shopify.git](https://github.com/mohammadhoseindehghani/Shopify.git)
cd Shopify
